apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongodb-pv
spec:
  capacity:
    storage: 500Mi

  accessModes:
    - ReadWriteOnce

  storageClassName: manual
  
  hostPath:
    path: /data/mongodb